---
layout: post
title: "Why agents fail on enterprise data (and what grounding actually takes)"
---

<!-- DRAFT OUTLINE — move to _posts/ with a date to publish. -->

Idea: a practitioner's view of the problem your team works on, written
publicly-safe (only reference shipped/announced features).

Possible sections:
- The demo-to-production gap: agents that ace SQL benchmarks flounder on a
  real warehouse. Why: schema sprawl, tribal semantics, permissions.
- Context engines vs. RAG: why "retrieve some docs" doesn't work for
  structured data; what business context / semantic layers add.
- Governance as a first-class constraint, not an afterthought.
- What evaluation looks like when correctness is a join, not a vibe.
